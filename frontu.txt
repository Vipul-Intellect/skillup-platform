==================================================
PHASE 2 — FRONTEND + COMPLETE INTEGRATION
==================================================

IMPORTANT:

Phase 1 backend work is already finalized and verified.

The backend is FROZEN during this phase.

This phase must make the EXISTING FRONTEND correctly consume and expose
the finalized backend behavior while preserving every existing feature
and user flow.

DO NOT modify backend code.

DO NOT redesign backend APIs.

DO NOT add backend fields.

DO NOT change backend logic.

==================================================
1. FIRST — INSPECT THE COMPLETE CURRENT REPOSITORY
==================================================

Before modifying frontend code:

Inspect the complete current repository and understand the actual
implementation.

At minimum identify:

- frontend framework and architecture
- all routes/pages
- all reusable components
- all API clients/services
- all API calls
- all request payloads
- all response parsing
- all state/store/hooks
- authentication flow
- YouTube flow
- learning flow
- Progress Dashboard flow
- Practice/Monaco flow
- executor flow
- evaluation flow
- navigation between these flows
- loading/error/empty states
- existing responsive behavior

Also inspect the finalized backend interfaces relevant to the frontend.

Do not rely only on old frontend interfaces, documentation, or
assumptions.

Trace actual code.

==================================================
2. DO NOT START CODING IMMEDIATELY
==================================================

After repository inspection:

FIRST provide a concise implementation plan.

The plan must identify:

- exact frontend files involved
- exact backend/API contracts being consumed
- current data flow
- existing fields already available
- required frontend changes
- existing integration issues discovered
- any frontend-only fixes required
- any backend limitation that prevents a correct frontend implementation

Then STOP.

Do not modify code until I explicitly approve the plan.

==================================================
3. PRODUCTION FRONTEND ENGINEERING
==================================================

Act as a senior production frontend engineer.

Do not wait for me to discover frontend integration problems manually.

While analyzing the complete repository, proactively identify issues such
as:

- incorrect API-to-UI mappings
- outdated response assumptions
- renamed/missing fields
- data available in state but unnecessarily requested again
- state lost during navigation
- existing context not passed to the next screen
- existing information unnecessarily requested from the user again
- broken existing actions
- incorrect button behavior
- incorrect navigation
- stale state
- duplicate API requests
- incorrect loading/error/empty handling
- broken responsive behavior
- existing features accidentally disconnected
- incorrect formatting of backend values

If a problem is clearly a FRONTEND problem, fix it using the existing
backend contract and existing application data.

Do not wait for the user to report these issues one by one.

This permission does NOT authorize new product features.

==================================================
4. YOUTUBE — FINALIZED BACKEND INTEGRATION
==================================================

Verify the actual current video-library rendering path.

Trace:

Existing topic/skill input
        ↓
Frontend request
        ↓
/api/videos
        ↓
Flask route
        ↓
existing MCP/recommendation path
        ↓
Gemini semantic evaluation
        ↓
backend deterministic scoring/ranking
        ↓
backend JSON response
        ↓
frontend parsing/state
        ↓
video cards

The frontend must correctly render the finalized backend fields where
they are actually present:

- video_id
- title
- thumbnail
- channel
- url
- duration
- description
- learning_fit_score
- concepts_covered
- why_recommended
- is_semantic_fallback

Preserve existing:

- title
- channel
- duration
- source/trusted indication
- thumbnail
- Watch on YouTube
- Use for Practice
- topic-selection flow
- current duration preference
- existing video-selection behavior

The frontend MUST NOT:

- calculate recommendation scores
- recalculate Learning Fit Score
- rank videos
- reorder recommendations based on frontend logic
- invent concepts
- invent recommendation reasons
- invent descriptions
- replace backend score with another score

The displayed Learning Fit Score must be the actual backend
learning_fit_score.

==================================================
5. SEMANTIC FALLBACK HANDLING
==================================================

If is_semantic_fallback is true:

- do not imply Gemini semantic evaluation succeeded
- do not fabricate concepts_covered
- do not fabricate why_recommended
- do not fabricate semantic explanations
- display only information actually returned by the backend
- handle missing semantic fields cleanly

Do not hide fallback behavior behind fake successful semantic content.

==================================================
6. LOAD MORE
==================================================

Initially display the finalized number of recommendations supported by
the existing backend contract.

Implement Load More ONLY if the ACTUAL verified backend contract and
existing application flow support additional retrieval/pagination/load
behavior cleanly.

Do not create fake client-side pagination.

Do not simply duplicate the same backend results.

If the backend does not provide a clean mechanism for additional unique
results, do not add Load More.

==================================================
7. PROGRESS DASHBOARD
==================================================

Verify the complete existing Progress Dashboard against the actual
available data.

Display only information that has a real source.

Where supported by the existing application, correctly display:

- learning progress
- skills/topics
- competency/mastery
- completed learning/practice
- assessment performance
- weak areas
- next steps
- progression/activity over time

Do NOT create:

- fake percentages
- fake progress
- fake mastery
- peer percentile
- population comparisons
- job-readiness percentage
- estimated job-readiness hours
- unsupported predictive metrics
- decorative statistics without real data

==================================================
8. PRACTICE / MONACO
==================================================

Preserve the complete existing practice flow.

Verify:

- problem selection
- language selection
- Monaco editor
- code state
- run
- submit
- executor request
- execution result
- tests
- output
- errors
- hints
- existing practice state
- navigation into/out of practice

Do not implement Gemini code evaluation in this phase.

Do not break the existing executor integration.

==================================================
9. EVALUATION
==================================================

Verify the existing evaluation UI against the actual backend response.

Preserve correctly supported fields such as:

- total_score
- badge
- readiness
- strengths
- weak_topics
- next_steps

If confidence exists, display it qualitatively as:

- High
- Medium
- Low

Do not call confidence "accuracy".

If independence/hint evidence already exists, display it only when backed
by actual application data.

Do not invent recruiter metrics.

Do not implement the new Gemini editor evaluator in this phase.

==================================================
10. EVERY EXISTING FEATURE MUST BE PRESERVED
==================================================

This frontend phase must account for ALL existing application
functionality, not only the newly modified YouTube UI.

Verify that existing behavior remains functional for:

- authentication
- navigation
- learning/topic selection
- video recommendations
- video selection
- Watch on YouTube
- Use for Practice
- practice/code editor
- language selection
- run
- submit
- executor results
- hints
- evaluation
- dashboard/progress
- existing API interactions
- existing state persistence
- existing error handling

Do not remove, silently disable, or replace an existing feature.

==================================================
11. DATA INTEGRITY — CRITICAL
==================================================

Every displayed field must have a real, traceable source.

For every displayed:

- text
- number
- percentage
- score
- badge
- status
- progress value
- recommendation reason
- statistic
- description
- competency value

verify:

Actual backend/application data
        ↓
Correct frontend variable/state
        ↓
Correct field mapping
        ↓
Correct transformation/formatting
        ↓
Correct displayed value

Do NOT:

- invent values
- estimate unsupported values
- hardcode dynamic values
- use mock values as real production data
- reuse unrelated fields
- duplicate one value under different labels
- display stale values
- infer unsupported metrics
- display meaningless empty values

If the application does not provide a value:

- do not invent it
- do not estimate it
- do not ask the user for it if equivalent existing data is already
  available elsewhere
- omit it or show an appropriate unavailable state

Do not display every backend field automatically.

Only display fields with a genuine user-facing purpose.

==================================================
12. COMPLETE BACKEND ↔ FRONTEND CONTRACT AUDIT
==================================================

After frontend implementation, verify every API-driven feature against
the ACTUAL finalized backend behavior.

For each API-driven flow verify:

Backend request contract
        ↓
Frontend request
        ↓
Backend processing
        ↓
Backend response
        ↓
Frontend response parsing
        ↓
Frontend state
        ↓
UI rendering
        ↓
User action
        ↓
Next existing workflow

Verify:

1. Request field names match exactly.
2. Response field names match exactly.
3. Data types match exactly.
4. Required fields are handled correctly.
5. Optional/null fields are handled safely.
6. Existing fields are not accidentally removed.
7. Existing fallback behavior is preserved.
8. Existing error behavior is preserved.
9. Existing selection state remains valid.
10. Existing navigation receives the correct context.
11. Use for Practice receives valid existing video data.
12. Watch on YouTube uses the actual backend URL/data.
13. Frontend never performs recommendation ranking.
14. Backend Learning Fit Score is the displayed score.
15. Backend recommendation reasons are displayed without fabrication.
16. Semantic fallback is represented correctly.
17. No duplicate recommendations are introduced.
18. No duplicate API calls are introduced unnecessarily.
19. No API breaking change was introduced.
20. No new dependency was added unless genuinely required.
21. No existing dependency was unnecessarily upgraded/downgraded.
22. No unrelated application behavior was changed.

==================================================
13. PRODUCTION USER-FLOW AUDIT
==================================================

Do not verify only that individual pages compile.

Walk through the actual existing user journeys.

For every major flow ask:

- What does the user click?
- What data does the frontend already know?
- Where does that data come from?
- Is it carried to the next step?
- Does the user unnecessarily need to re-enter it?
- Does the next screen receive the correct context?
- Does the API receive the correct request?
- Does the response map correctly?
- Does the UI display the correct value?
- Does the next action continue the existing workflow?

The user should not have to repeatedly navigate up/down, reselect,
re-enter, or reconstruct information that the existing application
already has available.

Fix such frontend integration problems when they can be fixed without
changing the frozen backend contract.

==================================================
14. VALIDATION
==================================================

Before declaring success, actually execute the applicable tests/checks.

At minimum:

A. Frontend starts successfully.
B. Frontend builds successfully.
C. Applicable type/lint checks pass.
D. Existing routes load.
E. Authentication still works.
F. Existing API calls execute correctly.
G. YouTube recommendations render using the actual backend response.
H. Learning Fit Score is displayed from backend data.
I. Concepts render from backend data.
J. Why Recommended renders from backend data.
K. Semantic fallback behaves correctly.
L. Watch on YouTube works.
M. Use for Practice works.
N. Progress Dashboard renders actual available data.
O. Practice/Monaco still works.
P. Run/Submit still works.
Q. Existing evaluation flow works.
R. Loading states work.
S. Error states work.
T. Empty states work.
U. Responsive behavior works.
V. No new console-breaking errors occur.
W. Existing functionality outside modified screens remains intact.

If a test fails:

1. Identify the actual root cause.
2. Make the smallest necessary FRONTEND correction.
3. Retest.
4. Do not introduce unrelated architecture changes.

Never claim a test passed unless it was actually executed.

==================================================
15. CHANGE DISCIPLINE
==================================================

For every changed frontend file, be able to explain:

1. Why it had to change.
2. What exact behavior changed.
3. Which existing feature or finalized backend contract required it.
4. Why it does not break existing functionality.

Prefer the smallest correct change.

Do not rewrite stable code without a concrete reason.

Do not add unnecessary abstractions.

Do not add unnecessary dependencies.

==================================================
16. FINAL REPORT
==================================================

At completion report:

1. Frontend framework identified.
2. Complete frontend areas inspected.
3. Backend/API contracts inspected.
4. Files changed.
5. Files deliberately not changed and why.
6. Existing components reused.
7. New components created, if any.
8. YouTube fields integrated.
9. Progress Dashboard fields actually available and displayed.
10. Practice/Monaco functionality verified.
11. Evaluation functionality verified.
12. Existing features verified as preserved.
13. Backend ↔ frontend compatibility results.
14. Tests/checks actually executed and results.
15. Responsive verification results.
16. Any remaining limitation supported by actual repository evidence.

For backend:

Report only:

- finalized backend behavior consumed
- backend contracts verified
- backend files deliberately left unchanged

Do NOT report backend modifications unless they actually occurred.

==================================================
17. ABSOLUTE BOUNDARIES
==================================================

Backend is FROZEN.

Do not modify:

- Flask APIs
- YouTube backend
- Gemini recommendation logic
- MCP tools
- A2A
- ADK agents
- Firestore
- executor service
- Docker
- AWS
- deployment configuration
- backend dependencies

Do not add new product features.

Do not invent data.

Do not invent metrics.

Do not invent API fields.

Do not invent user flows.

Do not silently change architectural decisions.

If a required frontend behavior genuinely cannot be implemented from
the finalized backend and existing application data:

STOP and report the exact limitation.

==================================================
MOST IMPORTANT
==================================================

ACT AS A SENIOR PRODUCTION AI/FRONTEND ENGINEER.

Understand the COMPLETE application before changing it.

Do not make the user discover integration problems one by one.

Automatically identify and fix legitimate FRONTEND integration issues.

Every displayed value must be real and traceable.

Every existing feature must remain functional.

Every finalized backend change that affects the frontend must be correctly
reflected in the frontend.

Prefer:

CORRECTNESS
TRACEABILITY
COMPATIBILITY
RELIABILITY
MINIMAL CHANGES

over:

COMPLEXITY
EXTRA FEATURES
DECORATION
SPECULATION

DO NOT START CODING UNTIL THE CURRENT REPOSITORY HAS BEEN INSPECTED AND
THE EXACT FILES AND DATA FLOWS REQUIRING CHANGES HAVE BEEN IDENTIFIED.





PLAN CORRECTION — REQUIRED BEFORE IMPLEMENTATION

The plan is approved in principle, but implementation must follow these
additional constraints:

1. Do not assume only static/js/app.js must change.
   After complete inspection, modify the minimum frontend files actually
   required.

2. Do not assume existing field mappings are correct.
   Verify every affected API field:

   backend response
   → frontend API parsing/state
   → UI rendering

3. Do not invent numeric-to-qualitative confidence thresholds.
   Only use an existing verified mapping or preserve the existing
   representation/report the limitation.

4. Audit all existing frontend flows affected by this work:
   authentication, topic selection, duration preference, YouTube,
   video selection, Watch on YouTube, Use for Practice, Practice/Monaco,
   executor interaction, evaluation, progress/dashboard, navigation,
   state, and API handling.

5. Every displayed field must have a real, traceable data source.
   No fabricated, estimated, hardcoded, duplicated, stale, or inferred
   values.

6. Do not automatically render every backend field.
   Display only fields with a legitimate user-facing purpose.

7. For YouTube:
   description → video.description
   concepts → video.concepts_covered
   Learning Fit Score → video.learning_fit_score
   Why Recommended → video.why_recommended
   fallback state → video.is_semantic_fallback

8. Frontend must never calculate, modify, or rank Learning Fit Scores.

9. If semantic fallback is true, do not fabricate semantic fields or
   imply that Gemini semantic evaluation succeeded.

10. Do not implement Load More unless the actual verified backend contract
    supports retrieving additional unique results cleanly.

11. If whole-project analysis discovers a genuine FRONTEND integration
    issue, fix it using the existing backend contract and existing data.
    Do not wait for me to identify it.

12. If an issue genuinely requires backend modification, STOP and report
    it. Do not modify the frozen backend.

13. Do not start implementation until the complete repository and actual
    affected data flows have been inspected and the exact files requiring
    changes are identified.

14. After implementation, execute the compatibility and regression
    checks. Do not claim any check passed unless it was actually run.

    ==================================================
VISUAL CONSISTENCY — MATCH THE EXISTING DESIGN EXACTLY
==================================================

The frontend modernization must look like a natural evolution of the
CURRENT SkillUp design, not a replacement with a different template.

Before making visual changes, inspect the ACTUAL current frontend design
and identify:

- existing color palette
- background colors
- surface/card colors
- primary/secondary/accent colors
- text colors
- muted text colors
- font family
- font sizes
- font weights
- heading hierarchy
- border styles
- border radius
- shadows
- button styles
- input/form styles
- badges
- status colors
- spacing scale
- layout/grid behavior
- navigation styling
- existing icons
- existing hover/focus/active states
- dark/light theme behavior if present

Use the existing design system and styling conventions wherever possible.

Do NOT invent a new color palette.

Do NOT arbitrarily change the existing font family.

Do NOT introduce unrelated typography.

Do NOT replace the current visual identity with a generic SaaS,
dashboard, AI, glassmorphism, or template-style design.

Any new or modified component must visually match the existing
application's:

- colors
- typography
- spacing
- borders
- radius
- shadows
- buttons
- badges
- states
- interaction patterns

The new YouTube fields, Progress Dashboard sections, and modified
Evaluation/Practice UI must look as though they were originally part
of the existing application.

==================================================
DESIGN PRESERVATION RULE
==================================================

Modernize through:

- better hierarchy
- better spacing
- cleaner grouping
- improved readability
- better responsive behavior
- consistent component reuse

Do NOT modernize by completely changing the visual language.

If an existing design token/style already solves the requirement,
reuse it instead of creating another one.

Before creating a new color, font, spacing value, border style, button
style, or component pattern, check whether an existing equivalent is
already present.

Avoid unnecessary duplicate CSS/design patterns.

==================================================
NO VISUAL DRIFT
==================================================

After implementation, compare the modified screens with the existing
application's visual language.

Verify that:

- colors remain consistent
- fonts remain consistent
- spacing remains consistent
- buttons remain consistent
- cards remain consistent
- navigation remains consistent
- badges/status indicators remain consistent
- existing page hierarchy remains recognizable
- existing branding remains intact

The result should feel like:

"the same SkillUp application, professionally improved"

NOT:

"a newly designed application placed on top of the old one."

Do not change visual elements that are unrelated to the requested
frontend improvements unless the change is required for consistency
across the existing application.