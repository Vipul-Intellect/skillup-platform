
as senior ai engineer without any assumptions ,hallucination,mistake and also without chaging any ocde as senior ai engineer
Implement Gemini as a fallback validator for the existing SkillUp Editor → Backend → Executor flow.

IMPORTANT: Do not disturb, refactor, or change any existing execution flow or unrelated functionality.

SCOPE
- Backend: primarily `tools/mcp_tools/code_executor.py`
- Frontend: `static/js/app.js` only if required to display the new result.
- Do not modify `executor_service`, agents, MCP architecture, database, deployment configuration, or add dependencies/endpoints unless absolutely required by the existing flow.

BEHAVIOR
1. Student clicks Run from the existing frontend editor.
2. Keep the current real Executor path unchanged.
3. If the Executor can run the submitted code, use it normally:
   - real output remains real execution output
   - genuine syntax/runtime/compile errors remain normal Executor errors
   - DO NOT call Gemini.
4. If the Executor internally determines that the language, runtime, compiler, library/dependency, or environment is unavailable:
   - DO NOT show that unsupported/environment error to the student.
   - Directly send the code to Gemini for validation.
5. Gemini must validate the submitted code against the student's actual requirement/task and language/context.
6. Gemini does NOT execute or compile the code.

GEMINI RESULT
Return structured data containing:
- `status`: exactly `VALID` or `INVALID`
- `errors`: list of concrete errors, including relevant source line numbers when identifiable
- `explanation`
- `suggestion`

For INVALID, explain what is wrong and where.
For VALID, errors must be empty.

Never claim that Gemini executed, compiled, or tested the code.

FRONTEND
- Preserve the existing editor UI/design.
- Real Executor results must continue appearing exactly as before.
- Gemini results must be clearly labeled `AI Code Validation`.
- Show `VALID` or `INVALID`.
- For INVALID, show Gemini's line-numbered errors, explanation, and suggestion.
- Never display Gemini validation as `Execution Successful`.
- Never expose the internal "unsupported language", missing dependency, compiler, or environment error that triggered the fallback.
- If Gemini fails/malformed response: show `AI Code Validation: Unavailable`; never fabricate a result.

SAFETY
- Never execute anything returned by Gemini.
- Keep the existing Gemini API key server-side.
- Do not route normal student syntax/runtime/logic errors to Gemini; Executor remains authoritative for actual execution.

TEST BEFORE FINISHING
Verify at minimum:
1. Supported Python → real Executor, no Gemini.
2. Unsupported language/environment → Gemini directly, internal unsupported error hidden.
3. Missing dependency → Gemini fallback, internal dependency error hidden.
4. Real syntax/runtime error → existing Executor error, no Gemini.
5. Gemini failure → graceful `AI Code Validation: Unavailable`.

Run actual available tests/syntax checks and report honestly what was tested.

FINAL HARD STOP:
Do not make any unrelated changes. Report modified files, fallback behavior, and test results, then stop.

EDITOR FORMATTING
- Add/improve automatic code indentation/formatting in the existing editor where appropriate.
- Preserve the current editor, styling, language behavior, and Run flow.
- Do not introduce a new editor library/dependency unless one already exists in the project.
- Formatting must not modify the actual code sent to the Executor/Gemini unexpectedly.
- Do not change or reformat code automatically in a way that alters its logic.
- If automatic formatting is not safely supported for a language, leave the code unchanged. 
strictly wothout disturbig or removig any other existing function or worflow